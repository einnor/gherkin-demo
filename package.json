{
  "name": "gherkin-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js features/**/*.feature --require-module ts-node/register --require features/stepDefinitions/**/*.ts --format json:gherkin-demo.test-results.json && cat gherkin-demo.test-results.json | cucumber-junit > gherkin-demo.test-results.xml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^7.0.0",
    "@types/node": "^14.14.31",
    "cucumber-junit": "^1.7.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
